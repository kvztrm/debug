-- FIXED: AccessoryType as STRING ("Hat", "Back", "Shoulder", "Hair", "Face", "Front", etc.) - NO Enum!
-- Confirmed particles from Roblox Wiki/Rolimons/Catalog. Run each separately.

-- Fiery Horns of the Netherworld (fire) - Hat
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Hat",
            AssetId = 215718515
        }
    }
})

-- Doomsekkar (green fire/smoke) - Hat
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Hat",
            AssetId = 132809431
        }
    }
})

-- Frozen Horns of the Frigid Planes (blue fire) - Hat
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Hat",
            AssetId = 74891470
        }
    }
})

-- Flaming Mohawk (fire) - Hair
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Hair",
            AssetId = 191101707
        }
    }
})

-- Golden Sparkling Wings (gold sparkles) - Back
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Back",
            AssetId = 417456127
        }
    }
})

-- TRY as Shoulder if Back fails
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Shoulder",
            AssetId = 417456127
        }
    }
})

-- Spring Fairy (sparkles) - Hat (common ID from lists)
game:GetService("ReplicatedStorage"):WaitForChild("BloxbizRemotes"):WaitForChild("CatalogOnApplyToRealHumanoid"):FireServer({
    {
        AccessoryData = {
            Order = 1,
            AccessoryType = "Hat",
            AssetId = 150381051
        }
    }
})
