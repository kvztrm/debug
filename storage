-- This code was generated by Cobalt
-- https://github.com/notpoiu/cobalt

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

print("Waiting for game to load...")

local player = Players.LocalPlayer
local basementStorages = workspace:WaitForChild("BasementStorages", 10)
if not basementStorages then warn("BasementStorages failed to load!") return end

local remotesFolder = ReplicatedStorage:WaitForChild("Remotes", 10)
local sellEvent = remotesFolder and remotesFolder:WaitForChild("RemoteEvent", 10)
if not sellEvent then warn("RemoteEvent failed to load!") return end

local handles = {}
for _, child in ipairs(basementStorages:GetChildren()) do
    local door = child:FindFirstChild("Door")
    if door then
        local handle = door:FindFirstChild("Handle")
        if handle and handle:IsA("BasePart") then table.insert(handles, handle) end
    end
end
if #handles == 0 then warn("No handles found! Stopping.") return end
print("Found " .. #handles .. " handles. Running main loop...")

-- Blacklist of items the bot should NEVER try to sell
local itemsToIgnore = {
    ["Shotgun Ammo Pack"] = true,
    ["Wallet"] = true, 
    ["AK47"] = true,
    ["Revolver"] = true,
    ["Speed Juice"] = true,
    ["Light Ammo Pack"] = true,
    ["Medium Ammo Pack"] = true,
    ["Knife"] = true,
}

-- Safely cleans names (e.g., "Wallet [1]" -> "Wallet")
local function getCleanName(toolName)
    local cleanStr = string.gsub(toolName, "%s*%[%d+%]", "")
    return cleanStr 
end

local function getMobPosition(mob)
    if mob:IsA("BasePart") then
        return mob.Position
    elseif mob:IsA("Model") then
        return mob.PrimaryPart and mob.PrimaryPart.Position or mob:FindFirstChildWhichIsA("BasePart") and mob:FindFirstChildWhichIsA("BasePart").Position
    end
    return nil
end

local function getLootPosition(loot)
    if loot:IsA("BasePart") then
        return loot.Position
    elseif loot:IsA("Model") then
        return loot.PrimaryPart and loot.PrimaryPart.Position or loot:FindFirstChildWhichIsA("BasePart") and loot:FindFirstChildWhichIsA("BasePart").Position
    end
    return nil
end

local function findPrompt(obj)
    if obj:IsA("ProximityPrompt") then return obj end
    for _, c in ipairs(obj:GetChildren()) do
        local found = findPrompt(c)
        if found then return found end
    end
    return nil
end

local function tweenToPos(targetPos)
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local dist = (targetPos - root.Position).Magnitude
    local dur = dist / 100
    local info = TweenInfo.new(dur, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
    local tween = TweenService:Create(root, info, {CFrame = CFrame.new(targetPos)})
    tween:Play()
    tween.Completed:Wait()
end

local function collectLootItem(loot, lootFolder)
    local prompt = findPrompt(loot)
    if not prompt then return false end
    for _ = 1, 5 do
        prompt:InputHoldBegin()
        task.wait(prompt.HoldDuration + 0.1)
        prompt:InputHoldEnd()
        task.wait(0.05)
        if not loot:IsDescendantOf(lootFolder) then return true end
    end
    return false
end

local mobsFolder = workspace:FindFirstChild("Mobs")
local storages = workspace:FindFirstChild("Storages")
local safeModel = storages and storages:GetChildren()[2] and storages:GetChildren()[2]:FindFirstChild("Contents") and storages:GetChildren()[2].Contents:FindFirstChild("Objects") and storages:GetChildren()[2].Contents.Objects:FindFirstChild("Safe")
local safeBox = safeModel and safeModel:FindFirstChild("Base") and safeModel.Base:FindFirstChild("Box")

local function sellAll(forceSell)
    if not sellEvent then return end

    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    
    local currentBackpack = player:FindFirstChild("Backpack")
    local toSellCount = 0
    
    if currentBackpack then
        for _, tool in ipairs(currentBackpack:GetChildren()) do
            if tool:IsA("Tool") then
                local cleanName = getCleanName(tool.Name)
                if not itemsToIgnore[cleanName] then 
                    toSellCount = toSellCount + 1 
                end
            end
        end
    end
    
    if char then
        for _, tool in ipairs(char:GetChildren()) do
            if tool:IsA("Tool") then
                local cleanName = getCleanName(tool.Name)
                if not itemsToIgnore[cleanName] then
                    toSellCount = toSellCount + 1
                end
            end
        end
    end
    
    if toSellCount > 0 or forceSell then
        print("Preparing to sell... Items detected: " .. toSellCount)
        
        local npcFolder = workspace:FindFirstChild("NPC")
        local npcByIndex = npcFolder and npcFolder:GetChildren()[3]
        local npcByName = npcFolder and npcFolder:FindFirstChild("Storage Collector")
        local targetNPC = npcByName or npcByIndex
        
        if targetNPC then
            local targetPos
            if targetNPC:IsA("Model") and targetNPC.PrimaryPart then
                targetPos = targetNPC.PrimaryPart.Position
            elseif targetNPC:FindFirstChild("HumanoidRootPart") then
                targetPos = targetNPC.HumanoidRootPart.Position
            elseif targetNPC:IsA("BasePart") then
                targetPos = targetNPC.Position
            end
            
            if targetPos then
                root.CFrame = CFrame.new(targetPos + Vector3.new(0, 0, 3), targetPos)
                task.wait(0.6) 
            else
                root.CFrame = CFrame.new(-677, 10, 12)
                task.wait(0.6)
            end

            print("Firing sell remotes...")
            if npcByIndex then
                sellEvent:FireServer("SellAllItems", npcByIndex, {})
            end
            if npcByName then
                sellEvent:FireServer("SellAllItems", npcByName, {})
            end
            
            print("Finished firing remotes.")
            task.wait(0.3)
        else
            warn("Could not find seller NPC in workspace!")
        end
    end
end

local function findToolByCleanName(parent, targetName)
    if not parent then return nil end
    for _, tool in ipairs(parent:GetChildren()) do
        if tool:IsA("Tool") and getCleanName(tool.Name) == targetName then
            return tool
        end
    end
    return nil
end

local function storeSpecial()
    local currentBackpack = player:FindFirstChild("Backpack")
    local char = player.Character
    if not char then return end
    
    local special = findToolByCleanName(currentBackpack, "Shotgun Ammo Pack") or findToolByCleanName(char, "Shotgun Ammo Pack")
    
    if special and safeBox and safeModel then
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChild("Humanoid")
        if not root or not humanoid then return end
        
        print("Storing Shotgun Ammo Pack...")
        local boxPos = safeBox.Position + Vector3.new(0, 2, 0)
        root.CFrame = CFrame.new(boxPos)
        task.wait(0.3)
        
        if special.Parent == currentBackpack then
            humanoid:EquipTool(special)
            task.wait(0.5)
        end
        
        sellEvent:FireServer("StoreBank", "5", "", safeModel)
        print("Stored!")
    end
end

while true do
    -- Mandatory yield to prevent crashing!
    task.wait(1) 
    
    local char = player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Humanoid").Health <= 0 then
        continue -- Safe to continue now since the wait is at the top
    end
    
    local root = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChild("Humanoid")
    if not root or not humanoid then continue end

    sellAll(false) 
    local opened = {}
    for _ = 1, #handles do table.insert(opened, false) end

    for i, handle in ipairs(handles) do
        if opened[i] then continue end
        if not player.Character or player.Character:FindFirstChild("Humanoid").Health <= 0 then break end

        local skip = false
        if mobsFolder then
            for _, mob in ipairs(mobsFolder:GetChildren()) do
                if mob and mob.Parent then
                    local mobPos = getMobPosition(mob)
                    if mobPos and (mobPos - handle.Position).Magnitude <= 80 then skip = true; break end
                end
            end
        end
        if skip then continue end

        root.CFrame = CFrame.new(handle.Position + Vector3.new(0, 2, 0))
        task.wait(0.5)

        local doorPrompt = handle:FindFirstChild("ProximityPrompt")
        if doorPrompt then
            doorPrompt:InputHoldBegin()
            task.wait(doorPrompt.HoldDuration + 0.1)
            doorPrompt:InputHoldEnd()
        end
        task.wait(1.5)

        local lootFolder = handle.Parent.Parent and handle.Parent.Parent:FindFirstChild("Contents") and handle.Parent.Parent.Contents:FindFirstChild("Loot")
        if lootFolder then
            local lootItems = lootFolder:GetChildren()
            table.sort(lootItems, function(a, b) 
                local pa = getLootPosition(a)
                local pb = getLootPosition(b)
                return pa and pb and pa.Y > pb.Y
            end)

            for _, loot in ipairs(lootItems) do
                if not player.Character or player.Character:FindFirstChild("Humanoid").Health <= 0 then break end
                local lootPos = getLootPosition(loot)
                if lootPos then
                    tweenToPos(lootPos + Vector3.new(0, 0.5, 0))
                    if collectLootItem(loot, lootFolder) then
                        print("Collected: " .. loot.Name)
                    end
                    task.wait(0.2)
                end
            end
        end

        if lootFolder then 
            sellAll(true) 
            storeSpecial() 
        end
        opened[i] = true
        task.wait(0.2)
    end
end
